name: Build AEReality APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install glslang and compile shader
        run: |
          sudo apt-get update
          sudo apt-get install -y glslang-tools
          mkdir -p assets/shaders
          glslangValidator -V shaders/aereality_core.comp -o assets/shaders/aereality_core.spv

      - name: Clean Gradle cache
        run: |
          rm -rf android/.gradle
          rm -rf android/app/build
          rm -rf android/build
          rm -rf ~/.gradle/caches/

      - name: Ensure Android XML Resources & Themes Exist
        run: |
          mkdir -p android/app/src/main/res/values
          mkdir -p android/app/src/main/res/values-night
          mkdir -p android/app/src/main/res/drawable
          mkdir -p android/app/src/main/res/mipmap-hdpi
          mkdir -p android/app/src/main/res/mipmap-mdpi
          mkdir -p android/app/src/main/res/mipmap-xhdpi
          mkdir -p android/app/src/main/res/mipmap-xxhdpi
          mkdir -p android/app/src/main/res/mipmap-xxxhdpi

          # Create styles.xml with LaunchTheme and NormalTheme
          cat << 'EOF' > android/app/src/main/res/values/styles.xml
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
                  <item name="android:windowBackground">@drawable/launch_background</item>
              </style>
              <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
                  <item name="android:windowBackground">?android:colorBackground</item>
              </style>
          </resources>
          EOF

          # Create launch_background.xml
          cat << 'EOF' > android/app/src/main/res/drawable/launch_background.xml
          <?xml version="1.0" encoding="utf-8"?>
          <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
              <item android:drawable="@android:color/black" />
          </layer-list>
          EOF

          # Create default launcher icon
          cat << 'EOF' > android/app/src/main/res/drawable/ic_launcher.xml
          <?xml version="1.0" encoding="utf-8"?>
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
              <solid android:color="#00E5FF" />
          </shape>
          EOF

          cp android/app/src/main/res/drawable/ic_launcher.xml android/app/src/main/res/mipmap-hdpi/ic_launcher.xml
          cp android/app/src/main/res/drawable/ic_launcher.xml android/app/src/main/res/mipmap-mdpi/ic_launcher.xml
          cp android/app/src/main/res/drawable/ic_launcher.xml android/app/src/main/res/mipmap-xhdpi/ic_launcher.xml
          cp android/app/src/main/res/drawable/ic_launcher.xml android/app/src/main/res/mipmap-xxhdpi/ic_launcher.xml
          cp android/app/src/main/res/drawable/ic_launcher.xml android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.xml

      - name: Install dependencies
        run: flutter pub get

      - name: Build release APK
        run: flutter build apk --release --no-tree-shake-icons --dart-define=VM_SERVICE_MAX_HEAP_SIZE=4096 --android-skip-build-dependency-validation

      # ✅ NEW STEP: Sign APK with valid Android v2/v3 signature so Package Installer doesn't silently dismiss
      - name: Sign APK for Device Installation
        run: |
          # Generate temporary release keystore
          keytool -genkey -v -keystore release.keystore -alias aereality -keyalg RSA -keysize 2048 -validity 10000 -storepass android -keypass android -dname "CN=Shaderly, OU=Mobile, O=AEReality, C=US"

          # Locate apksigner from Android SDK
          APKSIGNER=$(find $ANDROID_SDK_ROOT/build-tools -name apksigner | sort | tail -n 1)
          ZIPALIGN=$(find $ANDROID_SDK_ROOT/build-tools -name zipalign | sort | tail -n 1)

          # Align and sign
          $ZIPALIGN -f -v 4 build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/app-release-aligned.apk
          $APKSIGNER sign --ks release.keystore --ks-key-alias aereality --ks-pass pass:android --key-pass pass:android --out build/app/outputs/flutter-apk/Shaderly-signed.apk build/app/outputs/flutter-apk/app-release-aligned.apk
          $APKSIGNER verify build/app/outputs/flutter-apk/Shaderly-signed.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Shaderly-signed.apk
          path: build/app/outputs/flutter-apk/Shaderly-signed.apk
