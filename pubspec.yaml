name: aereality_studio
description: "Vulkan Hardware-Accelerated Video Color Grading & Anime WIS Suite"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffmpeg_kit_flutter_new_full_gpl: ^2.1.0
  video_player: ^2.8.2
  file_picker: ^8.0.0
  path_provider: ^2.1.2
  image: ^4.1.7
  ffi: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/shaders/aereality_core.spv
