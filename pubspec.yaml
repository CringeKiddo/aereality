name: aereality
description: "AEReality 32-Bit HDR Node Grading Engine"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  video_player: ^2.9.5        # <-- PINNED for copyCurrentFrame
  file_picker: ^8.1.0
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  shaders:
    - shaders/aereality_core.frag   # <-- BACK TO YOUR MAIN SHADER
